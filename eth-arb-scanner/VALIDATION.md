# Проверки перед публикацией

Обновление фактических итогов, 15 сентября 2026: **27 тестов прошли**. Проверено сохранение всех трёх источников симуляций, точная граница нулевого результата и порога, отрицательные итоги, выбор лучшей суммы на маршрут, отсутствие дублей, учёт исторического порога, исключение неканонических результатов и корректный отчёт при наличии только старых счётчиков. Таблица результатов добавляется автоматически к существующей SQLite-базе.

Обновление диагностики, 13 сентября 2026: **22 теста прошли**. Добавлены проверки контрольной симуляции при нуле кандидатов, сохранения её расписания при перезапуске, общего лимита симуляций, исключения неканонических результатов, метрик интервалов и доступности пулов, отрицательных ближайших маршрутов, разделения Telegram-сообщений и возобновления частично доставленной сводки. Проверены отсутствие повторного начисления пропущенных блоков при сбоях и отделение пустого ответа RPC от смены хеша. Далее приведён исходный прогон первой версии.

Живая проверка обновления: блок **25970853**, 23 пула / 9 токенов, 0 кандидатов. Независимая контрольная симуляция WBTC прошла: вход 0.005 WETH, результат после бюджета газа около **−0.000467 WETH**. Ближайший ценовой разрыв +0.044% превратился в −0.016% после комиссий. Пример не является прогнозом; транзакции не отправлялись.

Дата: 12 сентября 2026 года.

- 14 автоматических тестов на Node.js 22.19.0: округление, комиссии, фильтры токенов/пулов, расписание Berlin/DST, SQLite, повторная доставка Telegram, RPC 429, дневной лимит и распределение квоты по времени, привязка симуляции к блоку, повторная проверка и отбрасывание неканонического блока.
- `npm audit`: 0 известных уязвимостей на момент проверки. `tmp` закреплён через overrides на 0.2.7 из-за уязвимой транзитивной версии компилятора Solidity.
- Solidity runtime скомпилирован solc 0.8.37 для EVM Paris.
- На публичном Ethereum RPC проверены 24 адреса пулов через фабрики. Проверены state overrides и EIP-1898.
- На блоке **25962543** успешно выполнены семь типов полного обменного цикла: Uni V3→V3, V3→Uni V2, V3→Sushi V2, Uni V2→V3, Uni V2→Sushi V2, Sushi V2→Uni V2, Sushi V2→V3. Все пробные сделки на 0.005 WETH дали отрицательный результат после бюджета газа. Транзакции не отправлялись.
- Длительный локальный прогон HTTP + WebSocket: 179 успешных снимков, один временный сбой RPC с последующим восстановлением, 0 обрывов WS и 0 прибыльных кандидатов. Наблюдение выборочное: за период пропущено 88 блоков. Эти цифры не являются прогнозом доходности.
- Реальная отправка Telegram требует пользовательских токена и chat_id; обработчик проверен на имитации успешного ответа и ошибки API.
- Docker Engine локально недоступен. Для проверки сборки на Ubuntu добавлен workflow `.github/workflows/eth-arb-scanner.yml`; результат его запуска следует смотреть в GitHub Actions.

Автоматическая проверка не доказывает отсутствие любых ошибок. Следующий эксплуатационный шаг — запустить наблюдение на сервере со своими RPC/Telegram настройками и проверить первую сводку.

## 2026-09-17: gas diagnostics and bounded amount refinement

Added separate current-block fee estimates, recorded fee inputs and break-even total gas price. Conservative signal accounting is unchanged. Old rows remain readable without invented gas details. A single midpoint refinement shares the existing per-snapshot quota and configured amount bounds; all comparisons use the same block hash.

Validation: 31 local tests, including estimate/budget divergence, integer break-even rounding, legacy/new report records, loss and boundary cases, and a scanner integration test finding a better intermediate amount with exactly four calls. No live transaction or Telegram message was sent for this update. This local refinement does not guarantee an optimal amount; gas remains a model of the simulation probe, not a transaction receipt.
